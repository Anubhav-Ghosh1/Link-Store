import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["instagram.com", "www.instagram.com","www.google.com","api.dicebear.com"],
  },
};

export default nextConfig;
